@TODO: 2')  write a code compressor (removing all extra spacing/indentation)
            * bug with -cc and "" in a short comment of the source file
            * put a test for code compression
       2'') put other languages in tests (C, Java, C++, PHP?), one after the
            other
       3)   extract inter line indentation rules
       3')  externalize language dependent keywords to the CLI
       3'') apply indentation rules
       4)   extract intra line indentation rules
